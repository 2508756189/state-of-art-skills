name: Validate Skill Market

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run unit tests
        run: python scripts/test_build_market.py
      - name: Validate registry artifacts
        run: python scripts/build_market.py --check
